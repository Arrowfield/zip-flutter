import 'package:flutter/material.dart';

class AritcleCard extends StatelessWidget {
  const AritcleCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("AritcleCard"),
    );
  }
}
